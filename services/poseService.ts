// Pose detection is disabled in this early version to focus on manual placement.
export const initializePoseDetection = async () => {
  return null;
};

export const detectPose = async (image: HTMLImageElement) => {
  return null;
};